import type { Config } from "tailwindcss";

export default {
    darkMode: ["class"],
    content: [
    "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
  	extend: {
  		fontFamily: {
        sans: ["var(--font-inter)", "system-ui", "sans-serif"],
      },
      colors: {
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        primary: {
          DEFAULT: "hsl(var(--primary))",
          foreground: "hsl(var(--primary-foreground))",
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary))",
          foreground: "hsl(var(--secondary-foreground))",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        accent: {
          DEFAULT: "hsl(var(--accent))",
          foreground: "hsl(var(--accent-foreground))",
        },
        popover: {
          DEFAULT: "hsl(var(--popover))",
          foreground: "hsl(var(--popover-foreground))",
        },
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
        // Sudoku specific colors accessible via Tailwind
        'sudoku-cell-border': 'hsl(var(--sudoku-cell-border))',
        'sudoku-cell-bg': 'hsl(var(--sudoku-cell-bg))',
        'sudoku-cell-selected-bg': 'hsl(var(--sudoku-cell-selected-bg))',
        'sudoku-cell-highlight-bg': 'hsl(var(--sudoku-cell-highlight-bg))',
        'sudoku-cell-prefilled-text': 'hsl(var(--sudoku-cell-prefilled-text))',
        'sudoku-cell-prefilled-bg': 'hsl(var(--sudoku-cell-prefilled-bg))', // New color
        'sudoku-cell-user-text': 'hsl(var(--sudoku-cell-user-text))',
        'sudoku-cell-pencil-text': 'hsl(var(--sudoku-cell-pencil-text))',
        'sudoku-cell-invalid-bg': 'hsl(var(--sudoku-cell-invalid-bg))',
        'sudoku-cell-invalid-text': 'hsl(var(--sudoku-cell-invalid-text))',
        'sudoku-box-border': 'hsl(var(--sudoku-box-border))',
  		},
  		borderRadius: {
  			lg: 'var(--radius)',
  			md: 'calc(var(--radius) - 2px)',
  			sm: 'calc(var(--radius) - 4px)'
  		},
  		keyframes: {
  			'accordion-down': {
  				from: {
  					height: '0'
  				},
  				to: {
  					height: 'var(--radix-accordion-content-height)'
  				}
  			},
  			'accordion-up': {
  				from: {
  					height: 'var(--radix-accordion-content-height)'
  				},
  				to: {
  					height: '0'
  				}
  			}
  		},
  		animation: {
  			'accordion-down': 'accordion-down 0.2s ease-out',
  			'accordion-up': 'accordion-up 0.2s ease-out'
  		}
  	}
  },
  plugins: [require("tailwindcss-animate")],
} satisfies Config;
